version: '3.8'

services:
  file-transformer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: file-transformer
    ports:
      - "3300:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    # 如需持久化上传的文件，可开启以下挂载
    # volumes:
    #   - ./uploads:/app/uploads
